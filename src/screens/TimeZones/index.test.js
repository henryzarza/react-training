import { cleanup } from '@testing-library/react';

afterEach(cleanup);

describe('TimeZones', () => {
  test.todo('it shows loading');

  test.todo('it renders well');

  test.todo('it selects a specific field and shows it');
});
